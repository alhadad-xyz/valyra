{
    "name": "contracts",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "build": "echo 'Building contracts...'",
        "postbuild": "cd ../.. && pnpm sync-abis",
        "test": "echo 'Testing contracts...'",
        "deploy": "echo 'Deploying contracts...'"
    },
    "devDependencies": {
        "config": "workspace:*"
    }
}