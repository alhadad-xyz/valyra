# Valyra Frontend Environment Configuration

# Internet Computer Configuration
VITE_IC_HOST=http://localhost:4943
VITE_ENVIRONMENT=local

# Canister IDs (replace with actual deployed canister IDs)
VITE_LISTING_REGISTRY_CANISTER_ID=uzt4z-lp777-77777-qaabq-cai
VITE_OFFER_CANISTER_ID=offer-canister-id

# WebSocket Configuration (optional - for real-time offer updates)
# Leave empty to disable real-time features during development
# VITE_WS_URL=ws://localhost:8080

# Production WebSocket URL (when backend WebSocket server is deployed)
# VITE_WS_URL=wss://api.valyra.com